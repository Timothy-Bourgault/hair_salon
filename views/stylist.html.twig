<!DOCTYPE html>
<html>
  <head>
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
    <script src="/js/bootstrap.min.js" type="text/javascript"></script>
    <title>Stylist Page</title>
  </head>
  <body>
    <div class="container">
      <h1>{{ stylist.getName }}'s Stylist Profile</h1>
      <a href="/get_stylist_update/{{ stylist.getId }}">Click Here To Update This Stylist's Information</a>
      <hr>
      <hr>
      <div class="row">
        <div class="col-sm-6">
          <h1>Scheduled Days:</h1><h2>{{ stylist.getScheduledDays }}</h2>
        </div>
        <div class="col-sm-6">
          <h1>Specialties:</h1><h2>{{ stylist.getSpecialties }}</h2>
        </div>
      </div>
      <hr>
      <hr>
      <div class="row">
        <div class="col-sm-6">
          <h2>{{ stylist.getName }}'s Clients:</h2>
          <ul>
            {% if clients is not empty %}
              {% for client in clients %}
                <li><a href="/get_client/{{ stylist.getId }}/{{client.getId}}">{{ client.getName }}</a>
                </li>
              {% endfor %}
            </ul>
          {% else %}
            <h3>This Stylist currently has no Clients...</h3>
          {% endif %}
          <form action="/clients/delete_all" method="post">
            <button type="submit" class="btn btn-danger">Clear All Clients</button>
          </form>
        </div>
        <div class="col-sm-6">
          <h2>Add a New Client for this Stylist:</h2>
          <form action="/add_client" method="post">
            <label for="client_name">Name</label>
            <input id="client_name" type="text" name="client_name">
            <input type="hidden" name="stylist_id" value="{{ stylist.getId }}">
            <br>
            <br>
            <button class="btn-success btn-md" type="submit">Client Name</button>
          </form>
          <hr>
        </div>
      </div>
      <p>
        <a href="/">Go To Salon Home Page</a>
      </p>
    </div>
  </body>
</html>
